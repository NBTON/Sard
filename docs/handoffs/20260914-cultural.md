# Cultural integration handoff — 2026-09-14

Worker: Codex Luna cultural-integration implementation worker
Branch: `herdr/cultural-integration-repair-20260914`
Worktree: `C:\Users\nawaf\.herdr\worktrees\Sard_Agent\herdr-cultural-integration-repair-20260914`
Base: `bb042e8c0df3690caded98893b0fdb70a6a36cee`
Requested runtime: `gpt-5.6-luna`, reasoning `xhigh`

## Root cause

The existing calibrated lexical score measured proximity, not cultural identity. A single generic overlap could therefore surface an unrelated Fashion/Crafts or regional record as evidence. The planner also exposed internal citation IDs to the API even when the raw L0 record had the real URL. Deterministic rewrite sanitization discarded safe Arabic equivalents, and degraded server fallback code still contained topic-specific shrimp/springs answers. Direct-model history could carry an earlier topic into a later degraded turn.

## Implemented path

The path now remains:

`prompt → intent/classification → Isnād planning → RAG/local/web retrieval → L0 raw evidence → entity/region/mandate relevance → provenance score/decision → grounded answer or hedge → ChatService → API SSE → frontend parser`

`sard/rag/relevance.py` is the shared gate. Recognized entities are matched against the current query and evidence topic/anchor, explicit sector and region conflicts are rejected, and unknown queries require meaningful overlap. The gate annotates entity, region, mandate, matched-topic, and overlap metadata for downstream auditability. It is applied after bundled/Zvec/local fusion, again at the cultural router boundary, and again before planner evidence is exposed or generated.

The four supported cultural cases are covered by aliases and the bundled/local evidence path:

- Eastern-coast shrimp drying: existing curated coastal records.
- Al-Ahsa hot springs: existing curated springs records; therapeutic language is qualified as local belief/use, not medical evidence.
- Saudi coffee and majlis: the bundled culinary record.
- Southern bread: added the Asir Development Authority’s local-flavors record, paraphrased with provenance at <https://www.asda.gov.sa/ar/from-aseer/authenticity/local-flavors>.

Strong-only proposal gating now prevents recipe cards, artisan-craft outputs, and etiquette simulators from reaching the renderer without a strong, entity-matched, mandate-matched citation. Generic document requests retain their existing behavior. Planner citations recover the raw source URL and stable citation ID; the API emits only valid HTTP(S) citations and always preserves `session_id`/`run_id` in `done`.

## Changed files

- `sard/rag/relevance.py` — shared cultural relevance, medical qualification, and proposal-grounding policy.
- `sard/rag/bundled_retriever.py` — score-plus-relevance filtering.
- `sard/rag/query_rewriter.py` — safe deterministic-equivalent closure.
- `sard/rag/bundled_index.json` — southern-bread evidence record.
- `sard/agent/tools/cultural_tools.py` — fused RAG relevance gate.
- `sard/agent/cultural_router.py` — RAG/web relevance gate, Arabic/English grounded synthesis, medical qualification.
- `sard/agent/chat_service.py` — planner evidence gate, raw URL/citation recovery, history isolation, strong-only proposal gate.
- `sard/api/server.py` — citation contract recovery/validation and topic-free uncertainty fallback.
- `src/lib/api.ts` — typed `done` metadata includes `session_id` and `run_id`.
- `tests/agent/test_cultural_integration.py` — layered cultural/router/ChatService tests with boundary fakes.
- `tests/test_chat_integration_invariants.py` — fallback and SSE expectations updated.
- `tests/model_session/test_fallback.py` — fallback no longer claims shrimp without evidence.
- `tests/model_session/test_session_isolation.py` — deterministic-equivalent regression expectation updated.

No deployment manifests, package locks, workflows, `sard/application/demo.py`, or `sard/application/service.py` were edited.

## Validation

- Focused: `...python.exe -m pytest -q --basetemp .pytest-tmp-regression2 tests/model_session/test_session_isolation.py tests/agent/test_cultural_integration.py tests/rag/test_query_rewriter.py tests/test_chat_integration_invariants.py tests/model_session/test_fallback.py` → **55 passed, 1 warning, exit 0**.
- Ruff: `...python.exe -m ruff check sard/agent sard/rag sard/api/server.py tests/agent/test_cultural_integration.py tests/test_chat_integration_invariants.py tests/model_session/test_fallback.py` → **All checks passed**.
- Full Ruff: `...python.exe -m ruff check .` → **8 pre-existing findings**, all unused imports/variable in `evals/` (`evals/test_cultural_search_rag.py`, `evals/test_rag_13regions.py`); none are in the owned/touched scope.
- Frontend Node tests: `node --test tests/test_sse_parser.mjs tests/frontend/test_sse_parser_frontend.mjs tests/frontend/test_browser_scenarios_mock.mjs tests/frontend/test_artifact_download_matrix.mjs` → **61 passed** (5 core + 56 frontend cases).
- Syntax/JSON: compileall passed for touched Python paths; bundled index JSON parsed successfully with 15 documents and 1 southern-bread record.
- Full pytest final: **656 passed, 2 skipped, 3 failed, 1 warning** in 162.01s. The three failures are pre-existing packaged-demo/cache failures in `tests/application/test_step7_integration.py` and `tests/application/test_step8_hardening.py`; they require changes in the forbidden `sard/application/demo.py`/`service.py` scope. No cultural/API/SSE test failed.

## Limits / risks / shared needs

- Southern bread coverage is intentionally limited to the documented Asir/oven-bread statement; the assistant must not generalize one local preparation or name across all southern regions.
- Live Parallel Search and external model behavior were not used in tests; external boundaries remain faked in the layered tests.
- The planner pipeline itself was preserved; the final agent-side gate is defensive because legacy/test/external retrievers can still return sparse metadata.
- Full-suite demo/cache failures remain outside this owned scope and require the application owner to repair the packaged cache/service behavior.

## Coordinator takeover remediation (OpenCode Muse 1.3 xhigh, 2026-09-14/15)

Prior Codex worker left uncommitted remediation (snapshotted as `b71cb6b`).
Coordinator inspection found single-topic shrimp/springs retrieval at zero
hits (expanded-term denominator collapse) and 21 undiscriminating proposals
for the combined request. Fixed forward on the same branch:

- `sard/agent/tools/cultural_tools.py`: local-corpus match ratio uses the
  user's base terms as denominator; expansion terms are recall aids only.
- `sard/agent/proposals.py` (new): typed source-grounded proposals; mandate
  gates require distinguishing capabilities evidenced within the authoritative
  mandate record; product selected from the mandate's `product_dimension`;
  per-topic isolation via scoped evidence dimensions (empty query string).
- `sard/rag/relevance.py`: strict token matching for the `بن` alias
  (بنفحاته/البنت/البنوك/لبن no longer count as coffee); coffee anchors
  require strong aliases; southern provinces split (Asir/Jazan/Najran);
  coastal aliases for Eastern; metadata-only region keys; national-practice
  scoping (coffee) vs fail-closed fallback for unseen topics.
- `sard/planner/retrieve.py`: live web hits pass the shared relevance gate
  before synthesis.
- `sard/agent/chat_service.py`: planner post-filter trims disallowed sources
  and clears synthesis only when a requested topic loses all support;
  same-session history restore shared by both paths (`extract_session_history_turns`,
  `resolve_followup_retrieval_query`); client `role="system"` never promoted.
- `sard/agent/cultural_router.py`: deterministic multi-topic synthesis emits
  one grounded section per topic instead of collapsing to the top hit.
- `sard/api/server.py`: SSE emits a typed `proposals` event and
  `proposals_count` in `done`; absence of the event means fail-closed.
- Tests: `tests/agent/test_cultural_acceptance.py` (13) and
  `tests/test_chat_sse_cultural.py` (4) cover normalization, paraphrases,
  reordered topics, province isolation, cross-topic isolation, unsupported-org
  exclusion, abstention, timeouts, history, system-role injection, real
  bundled/local retrieval, and the public `/api/chat` SSE path.

Validation at commit: focused suites 117 passed / 1 skipped; `ruff check
sard/ tests/` clean; combined request yields 4 topic citations + mandate
citations and 6+ proposals spanning all four topics with no
fashion/museum/film leakage; follow-up resolves antecedent same-session and
fails closed cross-session.

## Commit status

See branch log for remediation commits on `herdr/cultural-integration-repair-20260914`.
